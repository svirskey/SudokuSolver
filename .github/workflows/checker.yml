name: sudoku_solver checker

on:
  push:
    branches:
      - master
      - adding_gtest
  pull_request:
    branches:
      - master

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make test